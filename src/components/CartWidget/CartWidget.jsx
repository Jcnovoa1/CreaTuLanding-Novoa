// src/components/CartWidget/CartWidget.jsx
const CartWidget = () => {
  return (
    <div className="cart-widget">
      🛒
      <span>0</span> {/* Hardcodeamos el número por ahora */}
    </div>
  );
};

export default CartWidget;